12번째줄 Scanner를 이용하여 int 형식을 받아 와야하는데 next()은 int형이 아닌 문자(단어)를 가져오는 메서드이기때문에 오류가 발생한다.
조치사항 : int input = sc.nextInt(); 로 수정해야한다.

14번째줄 2 이상이고 9 이하의 정수만 입력해야하는데 if문 조건식에 input<=9라고만 쓰여져 있어 9이하의 숫자 무엇을 입력했을 때도 구문이 성립이 된다.
조치사항 : if(input>=2 && input <=9) 으로 조건식을 변경한다.

16번째 줄 쌍따옴표 안에 쓰여진 형식보다 변수의 갯수가 적어 오류가 발생한다.
조치 사항 : 형식에 맞추어 변수의 개수를 알맞게 집어넣어야한다.
System.out.printf("%d * %d = %d\n",input, num, input*num); 로 수정하여 두 수의 곱셈 출력하는 구문을 작성한다.

while문에 쓰인 조건문이 true로 쓰여져 무한 반복을 하기때문에 조건에 충족했을 경우 반복문을 빠져나오는 구문을 작성해야한다.
조치사항 : 14번째 줄에 쓰인 if문 안 쪽 17번째 줄에 break; 구문을 추가해준다.